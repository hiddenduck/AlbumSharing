syntax = "proto3";

// package file;

option go_package = "cbCastProtobuf/";

message CbCastMessage {
    
    MessageType Type = 1;

    repeated uint64 VersionVector = 2;

    bytes data = 3;
}

enum MessageType {
    FORWARD_MSG = 0;
}
